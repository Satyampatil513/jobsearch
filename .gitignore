__pycache__/
*.pyc
.pytest_cache/

# jobsearch.db is committed (not gitignored) because this project runs as a
# cloud Routine — each run starts from a fresh clone, so state must persist
# in the repo. See docs/design.md Phase 4.
